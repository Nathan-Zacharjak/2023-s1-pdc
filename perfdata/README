Each file contains data from the program
it is named after.

In each block of data, first is the number of cores used,
then the 3 times recorded for each test/run
of the program,
then is the average of those 3 times

Each block is for a number of threads requested
and each file has 3 blocks of data,
1 for 1 thread
1 for 2 threads
1 for 4 threads